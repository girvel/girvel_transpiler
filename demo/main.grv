include "stdio.h"

struct Vector {
    int x;
    int y;
}

int max(int a, int b) {
    if a > b {a} else {b}
}

int main() {
    printf("Hello, %d!\n", max(3, 4));
    Vector v;
    v.x = 3;
    printf("v.x is %d\n", v.x);
    0
}