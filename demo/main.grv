include "stdio.h"

struct Vector {
    int x;
    int y;
}

// TODO returns
Vector Vector::one() {
    Vector {1, 1}
}

int max(int a, int b) {
    if a > b {a} else {b}
}

int main() {
    printf("Hello, %d!\n", max(3, 4));
    printf("v.x is %d\n", Vector::one().x);
    0
}