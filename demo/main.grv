include "stdio.h"

struct Vector {
    int x;
    int y;
}

Vector Vector::one() {
    return Vector {1, 1};
    Vector {0, 0}
}

int max(int a, int b) {
    if a > b {a} else {b}
}

int main() {
    int x = 3;

    printf("Hello, %d!\n", max(3, 4));
    printf("v.x is %d\n", Vector::one().x);
    printf("x is %d\n", x);
    0
}