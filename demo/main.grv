include "stdio.h"
include<T: int> "./vector.grv"
// TODO .h files generation

main: int() => {
    mut Vector<int> v = Vector<int>::one();
    printf("v.y is %d\n", v.y);

    v = Vector<int> {3, 2};
    printf("v.y is %d\n", v.y);
    
    0
}