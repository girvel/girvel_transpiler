include "stdio.h"
include<T: int> "./vector.grv"
// TODO .h files generation

main: int() => {
    Vector<int> v = Vector<int>::one();
    printf("v.y is %d\n", v.y);
    char c = '!';
    printf("%c\n", c);
    0
}